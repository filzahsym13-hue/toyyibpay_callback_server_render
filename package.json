{
  "name": "toyyibpay_callback_server_render",
  "version": "1.0.0",
  "description": "ToyyibPay callback server for Render and Android emulator testing (zero-dependency HTTP core, optional dotenv/node-fetch).",
  "main": "index.js",
  "license": "MIT",
  "private": true,
  "scripts": {
    "start": "node index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "dotenv": "^16.4.5",
    "node-fetch": "^3.3.2"
  }
}

